package com.avito.android.test.checks

import android.support.test.espresso.web.sugar.Web

class WebElementChecks(private val interaction: Web.WebInteraction<Void>) {

    fun isDisplayed() {
        interaction
    }
}
